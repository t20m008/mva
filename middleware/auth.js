export default function ({ store, route, redirect }) {
  // eslint-disable-next-line no-console
  // console.log('middleware run:')
  // if (store.getters.isLoggedIn && route.name === 'login') {
  //   console.log('auth.js:2:', store.getters.isLoggedIn, route.name)
  //   return redirect('/')
  // }
  // if (!store.getters.isLoggedIn && route.name === 'logout') {
  //   console.log('auth.js:3:', store.getters.isLoggedIn, route.name)
  //   return redirect('/login')
  // }
  // if (!store.getters.isLoggedIn && route.name === 'index') {
  //   console.log('auth.js:4:', store.getters.isLoggedIn, route.name)
  //   return redirect('/login')
  // }
}
